// If you want to use Phoenix channels, run `mix help phx.gen.channel`
// to get started and then uncomment the line below to connect to your
// channels:
//
//   import "./user_socket.js"

// Bundled by esbuild. The phoenix / phoenix_html / phoenix_live_view JS
// packages are resolved from deps via NODE_PATH (see config/config.exs).
import "phoenix_html"

// Example channels client wiring — uncomment and adapt when the frontend
// starts consuming the RoomChannel:
//
//   import { Socket } from "phoenix"
//   const socket = new Socket("/socket", { params: { token: window.userToken } })
//   socket.connect()
